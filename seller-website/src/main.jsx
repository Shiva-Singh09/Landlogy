import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { ArrowUpRight, Bell, Check, ChevronRight, CircleHelp, Download, Eye, EyeOff, FileText, KeyRound, LayoutDashboard, LogOut, Menu, X, Phone, Mail, MapPin, Clock, MessageCircle, Search, ShieldCheck, TrendingUp, Building2, Home, Landmark, Users, Scale, Send, UserRound } from 'lucide-react';
import './styles.css';
import logo from './assets/Logo.png';
import propertyImage from './assets/Hero1.png';



const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000';
const heroImage = 'https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=2200&q=88';

const CLIENT_MOCK = {
    name: 'Aarav Mehta',
    email: 'aarav.mehta@example.com',
    phone: '+91 98XXX 21XXX',
    city: 'Mumbai, Maharashtra',
    accountId: 'CL-2026-0148',
    property: {
        title: 'Mehta Residence',
        type: 'Independent House',
        location: 'Baner, Pune',
        reference: 'LLY-P-0148',
        price: '₹2.85 Cr',
        status: 'under_review',
        updated: '12 September 2026',
        image: propertyImage
    },
    documents: [
        { name: 'Property documents', type: 'Verification file', date: '08 Sep 2026', status: 'Received' },
        { name: 'Owner information', type: 'Client record', date: '06 Sep 2026', status: 'Received' },
        { name: 'Property brief', type: 'Listing details', date: '05 Sep 2026', status: 'In review' }
    ]
};



function useReveal(enabled = true, rescan = '') { useEffect(() => { if (!enabled) return; const els = [...document.querySelectorAll('[data-reveal]')].filter(e => !e.classList.contains('is-visible')); const o = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add('is-visible'); o.unobserve(e.target) } }), { threshold: .12, rootMargin: '0px 0px -8% 0px' }); els.forEach(e => o.observe(e)); return () => o.disconnect() }, [enabled, rescan]) }
function useCounters(enabled = true) { useEffect(() => { if (!enabled) return; const els = document.querySelectorAll('.counter'); const parse = v => Number(v.replace(/[^0-9.]/g, '')); const obs = new IntersectionObserver(es => es.forEach(e => { if (!e.isIntersecting || e.target.dataset.done) return; e.target.dataset.done = '1'; const raw = e.target.dataset.target, n = parse(raw), prefix = raw.trim().startsWith('₹') ? '₹' : '', suffix = raw.includes('Cr') ? 'Cr+' : raw.includes('%') ? '%' : raw.includes('+') ? '+' : ''; let start = 0; const t0 = performance.now(); const tick = now => { const p = Math.min((now - t0) / 1300, 1), ease = 1 - Math.pow(1 - p, 3); e.target.textContent = prefix + (n % 1 ? n * ease : (Math.round(n * ease))) + suffix; if (p < 1) requestAnimationFrame(tick) }; requestAnimationFrame(tick) }), { threshold: .7 }); els.forEach(e => obs.observe(e)); return () => obs.disconnect() }, [enabled]) }
function useSiteData() { const [site, setSite] = useState(null), [error, setError] = useState(null); const load = useCallback(() => { setError(null); fetch('/site-data.json').then(r => { if (!r.ok) throw new Error('HTTP ' + r.status + ' ' + r.statusText); return r.json() }).then(j => { console.log('[LANDLOGY] site-data.json loaded successfully'); setSite(j) }).catch(err => { console.error('[LANDLOGY] Failed to load /site-data.json. Make sure client/public/site-data.json exists and is served at /site-data.json.', err); setError(err) }) }, []); useEffect(() => { load() }, [load]); return { site, error, load } }
function Magnetic({ children, className = '', ...p }) { const r = useRef(); const move = e => { if (matchMedia('(pointer:fine)').matches) { const b = r.current.getBoundingClientRect(); r.current.style.transform = `translate(${(e.clientX - b.left - b.width / 2) * .12}px,${(e.clientY - b.top - b.height / 2) * .12}px)` } }; const leave = () => r.current && (r.current.style.transform = ''); return <a ref={r} onMouseMove={move} onMouseLeave={leave} className={`btn ${className}`} {...p}>{children}</a> }
function Form({ hero = false }) {
    const [status, setStatus] = useState(''), [errorMsg, setErrorMsg] = useState(''), [errors, setErrors] = useState({}), [busy, setBusy] = useState(false); const formRef = useRef(null);
    const validate = (data) => { const e = {}; const nm = String(data.name || '').trim(); if (!nm) e.name = 'Please enter your full name.'; else if (nm.length < 2 || nm.length > 80) e.name = 'Name must be 2–80 characters.'; else if (!/[\p{L}]/u.test(nm)) e.name = 'Please enter a valid name (letters only).'; if (!/^(?:\+91\s?)?[6-9]\d{9}$/.test(String(data.phone || '').replace(/\s+/g, ''))) e.phone = 'Please enter a valid 10-digit Indian mobile number.'; if (data.email && String(data.email).trim()) { const em = String(data.email).trim(); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(em)) e.email = 'Please enter a valid email address.'; else if (em.length > 160) e.email = 'Email is too long.'; } const ci = String(data.city || '').trim(); if (!ci) e.city = 'Please enter your city or location.'; else if (ci.length > 120) e.city = 'City is too long.'; const intentField = hero ? 'property_type' : 'intent'; if (!String(data[intentField] || '').trim()) e[intentField] = 'Please select an option.'; if (hero) { if (data.message && String(data.message).length > 2000) e.message = 'Message is too long (max 2000 characters).'; } else { const ms = String(data.message || ''); if (!ms.trim()) e.message = 'Please enter your message.'; else if (ms.trim().length < 10) e.message = 'Message must be at least 10 characters.'; else if (ms.trim().length > 2000) e.message = 'Message is too long (max 2000 characters).'; } return e; };
    const focusFirstInvalid = e => { if (!formRef.current || !Object.keys(e).length) return; const firstKey = Object.keys(e)[0]; const el = formRef.current.querySelector(`[name="${firstKey}"]`); if (el && typeof el.focus === 'function') el.focus(); };
    const clearError = n => setErrors(p => { if (!(n in p)) return p; const c = { ...p }; delete c[n]; return c });
    const submit = async e => {
        e.preventDefault(); setBusy(true); setStatus(''); setErrorMsg(''); setErrors({}); const form = e.currentTarget; const fd = new FormData(form); const data = Object.fromEntries(fd);
        const vErrs = validate(data); if (Object.keys(vErrs).length > 0) { setErrors(vErrs); focusFirstInvalid(vErrs); setBusy(false); return }
        data.formType = hero ? 'property-enquiry' : 'contact-message'; if (hero) data.intent = 'Sell my Property'; try { const res = await fetch(`${API_BASE}/api/enquiries`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); const result = await res.json(); if (!res.ok) throw new Error(result.error || 'Submission failed'); setStatus('success'); if (form && typeof form.reset === 'function') form.reset() } catch (err) { setStatus('error'); setErrorMsg(err.message) } finally { setBusy(false) }
    };
    return <form ref={formRef} onSubmit={submit} noValidate className="form">
        <div className="field-row"><Field label={hero ? 'Your Name' : 'Full Name *'} name="name" placeholder="Enter your full name" required autocomplete="name" error={errors.name} clearError={clearError} /> <Field label={hero ? 'Mobile' : 'Mobile Number *'} name="phone" placeholder="98765 43210" required inputmode="tel" autocomplete="tel-national" error={errors.phone} clearError={clearError} /></div>
        {hero && <Field label="Email Address" name="email" type="email" placeholder="you@example.com" autocomplete="email" error={errors.email} clearError={clearError} />}
        {!hero && <Field label="Email Address" name="email" type="email" placeholder="you@example.com" autocomplete="email" error={errors.email} clearError={clearError} />}
        <Field label={hero ? 'Property Type' : 'Enquiry Type'} name={hero ? 'property_type' : 'intent'} select options={hero ? ['Residential Apartment', 'Independent House / Villa', 'Plot / Land', 'Commercial Space', 'Office Space', 'Warehouse / Industrial'] : ['Sell my Property', 'Property Assessment', 'Pricing & Positioning', 'Verification Support', 'Professional Presentation', 'Marketing & Buyer Reach', 'Negotiation & Closing Support', 'Other']} placeholder={hero ? 'Select property type' : 'Select a seller-service enquiry'} error={errors[hero ? 'property_type' : 'intent']} clearError={clearError} />
        <Field label="City / Location of Interest" name="city" placeholder="Enter your city or location" autocomplete="address-level2" error={errors.city} clearError={clearError} />
        {!hero && <Field label="Your Message" name="message" textarea placeholder="Tell us how we can help you..." autocomplete="off" error={errors.message} clearError={clearError} />}
        {hero && <Field label="Message (optional)" name="message" textarea placeholder="Tell us how we can help you..." autocomplete="off" error={errors.message} clearError={clearError} />}
        {status === 'success' && <div className="form-status success" role="status">✓ Your enquiry has been received and will be reviewed by LANDLOGY.</div>}
        {status === 'error' && <div className="form-status error" role="alert">✗ {errorMsg}</div>}
        <button type="submit" className="btn btn-primary submit-btn" disabled={busy}>{busy ? 'Sending...' : hero ? 'Send Enquiry' : 'Send Message'}</button>
    </form>
}
function Field({ label, name, select, options, textarea, type = 'text', placeholder, error, clearError, autocomplete, inputmode, ...p }) { const errId = `${name}-err`; return <div className={`field-group ${error ? 'has-error' : ''}`}><label htmlFor={name}>{label}</label>{select ? <select id={name} name={name} defaultValue="" onChange={clearError ? () => clearError(name) : undefined} aria-invalid={!!error} aria-describedby={error ? errId : undefined} {...p}>{<option value="">{placeholder || 'Select an option...'}</option>}{options?.map(o => <option key={o} value={o}>{o}</option>)}</select> : textarea ? <textarea id={name} name={name} placeholder={placeholder} onChange={clearError ? () => clearError(name) : undefined} aria-invalid={!!error} aria-describedby={error ? errId : undefined} {...p} /> : <input id={name} name={name} type={type} placeholder={placeholder} inputMode={inputmode} autoComplete={autocomplete} onChange={clearError ? () => clearError(name) : undefined} aria-invalid={!!error} aria-describedby={error ? errId : undefined} {...p} />}{error && <span id={errId} className="field-error" role="alert">{error}</span>}</div> }
function PublicSite() {
    const [menu, setMenu] = useState(false), [filter, setFilter] = useState('all'); const { site, error, load } = useSiteData(); const visibleProps = useMemo(() => site ? (site.properties || []).filter(p => filter === 'all' || p.category === filter) : [], [site, filter]); useReveal(!!site, filter); useCounters(!!site); useEffect(() => { let raf = 0; const onScroll = () => { if (raf) return; raf = requestAnimationFrame(() => { raf = 0; if (matchMedia('(pointer:fine)').matches) document.documentElement.style.setProperty('--sy', `${scrollY * .12}px`); const depth = Math.min(Math.max(scrollY / (window.innerHeight || 800), 0), 1); document.documentElement.style.setProperty('--scroll-depth', depth.toFixed(4)); document.body.classList.toggle('has-scroll-depth', matchMedia('(max-width:860px)').matches && !matchMedia('(prefers-reduced-motion:reduce)').matches) }) }; const onMouse = e => { if (!matchMedia('(pointer:fine)').matches) return; const nx = (e.clientX / window.innerWidth - .5) * 2; const ny = (e.clientY / window.innerHeight - .5) * 2; document.documentElement.style.setProperty('--mx', nx.toFixed(4)); document.documentElement.style.setProperty('--my', ny.toFixed(4)); }; onScroll(); addEventListener('scroll', onScroll, { passive: true }); addEventListener('resize', onScroll, { passive: true }); addEventListener('mousemove', onMouse, { passive: true }); return () => { removeEventListener('scroll', onScroll); removeEventListener('resize', onScroll); removeEventListener('mousemove', onMouse) } }); useEffect(() => { document.body.classList.toggle('menu-open', menu); const onKey = e => { if (e.key === 'Escape') setMenu(false) }; const onResize = () => { if (window.innerWidth > 860) setMenu(false) }; addEventListener('keydown', onKey); addEventListener('resize', onResize); return () => { document.body.classList.remove('menu-open'); removeEventListener('keydown', onKey); removeEventListener('resize', onResize) } }, [menu]); if (!site) return <div style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#0f1723', color: '#fff', textAlign: 'center', padding: 24 }}>{error ? <div style={{ maxWidth: 540 }}><div className="brand-mark" style={{ width: 48, height: 48, margin: '0 auto 18px' }}>L</div><h3 style={{ font: '600 24px/1.2 var(--serif)', margin: '0 0 8px' }}>Unable to load site data.</h3><p style={{ fontSize: 13, opacity: .7, margin: '0 0 10px' }}>Check that <code style={{ background: 'rgba(255,255,255,.14)', padding: '2px 6px', borderRadius: 6 }}>client/public/site-data.json</code> exists and is served at <code style={{ background: 'rgba(255,255,255,.14)', padding: '2px 6px', borderRadius: 6 }}>/site-data.json</code>.</p><p style={{ fontSize: 12, opacity: .5, margin: '0 0 20px' }}>Details were logged to the browser console.</p><button className="btn btn-primary" onClick={load}>Retry</button></div> : <div><div className="brand-mark" style={{ width: 48, height: 48, margin: '0 auto 18px' }}>L</div><p style={{ fontSize: 13, opacity: .7 }}>Loading LANDLOGY...</p></div>}</div>; return <><nav><div className="container nav-inner"><a className="brand" href="#"><img src={logo} alt="Logo" height='90vh' /></a><div className="navlinks">{[['services', 'Services'], ['listings', 'Properties'], ['intelligence', 'Intelligence'], ['network', 'Who We Serve'], ['team', 'Team'], ['contact', 'Contact']].map(([a, b]) => <a href={'#' + a} key={a}>{b}</a>)}</div><div className="nav-cta"><a href={`tel:${site.contact.phoneRaw}`} className="btn btn-outline"><Phone size={15} /> Call Us</a><Magnetic href="#contact" className="btn-primary">Enquire Now <ArrowUpRight size={16} /></Magnetic></div><button className="ham" type="button" onClick={() => setMenu(v => !v)} aria-label={menu ? 'Close menu' : 'Open menu'} aria-expanded={menu} aria-controls="landlogy-mobile-menu">{menu ? <X /> : <Menu />}</button></div></nav>{menu && <div className="mobile-menu" id="landlogy-mobile-menu" role="navigation" aria-label="Mobile navigation">{[['services', 'Services'], ['listings', 'Properties'], ['intelligence', 'Investment Intelligence'], ['network', 'Who We Serve'], ['team', 'Our Team'], ['contact', 'Contact Us']].map(([a, b]) => <a onClick={() => setMenu(false)} href={'#' + a} key={a}>{b}</a>)}</div>}
        <header className="hero"><div className="hero-media" /><div className="hero-gridlines" /><div className="container hero-inner"><div className="hero-content" data-reveal><div className="eyebrow">Research · Verify · Transact · Grow</div><h1>India's trusted<br />real estate<br /><span>intelligence</span> platform</h1><p className="hero-sub">From the first land search to final transaction — LANDLOGY connects buyers, sellers, tenants, investors, developers and brokers across India on one verified platform.</p><div className="hero-actions"><Magnetic href="#listings" className="btn-primary">Browse Properties <ArrowUpRight size={17} /></Magnetic><Magnetic href="#contact" className="btn-outline">Talk to an Expert <ChevronRight size={16} /></Magnetic></div><div className="hero-stats">{site.heroStats.map((s) => <div className="hstat" key={s.label}><strong>{s.value}</strong><span>{s.label}</span></div>)}</div></div><div className="hero-card tilt" data-reveal><div className="hero-card-title">Quick Property Enquiry</div><Form hero /></div></div></header>
        <section id="services"><div className="container"><Head eyebrow="One Platform" title="Everything real estate, connected." text="From your first property search to closing the deal — and every service in between — LANDLOGY is built as a complete, research-first real estate ecosystem." /><div className="services-grid">{site.services.map((s, idx) => <article className="svc-card tilt" data-reveal key={s.title}><div className="svc-icon">{s.icon}</div><h3>{s.title}</h3><p>{s.desc}</p><a className="svc-link" href={s.title.includes('Intelligence') || s.title.includes('Research') ? '#intelligence' : '#contact'}>{s.linkLabel} <ArrowUpRight size={14} /></a><span className="card-index">0{idx + 1}</span></article>)}</div></div></section>
        <div className="band"><div className="container band-grid">{(site.numbersBand.items || []).map((b) => <div className="bnum" data-reveal key={b.label}><strong className="counter" data-target={b.value}>{b.value}</strong><span>{b.label}</span></div>)}</div></div>
        <section id="intelligence"><div className="container intel-grid"><div data-reveal><div className="eyebrow">For Investors</div><h2>Intelligence before investment.</h2><p className="muted intro">Most real estate decisions are driven by listings and opinions. We lead with research, verification and structured analysis — so you act with confidence, not guesswork.</p><div className="intel-steps">{[['Market Intelligence', 'Understand locations, pricing, demand-supply dynamics, growth corridors and infrastructure impact.'], ['Opportunity Screening', 'Evaluate property fundamentals, legal documents, ownership status and risk factors — before you commit.'], ['Investment Analysis', 'Compare projected returns, yield, capital appreciation potential, liquidity and cost structures.'], ['Confident Decision', 'Connect with the right professionals and move from research to transaction with clarity.']].map(([t, p], i) => <div className="istep" data-reveal key={t}><div className="istep-num">{i + 1}</div><div><h3>{t}</h3><p>{p}</p></div></div>)}</div></div><div className="intel-right" data-reveal><div className="intel-card"><div className="mini-orbit" /><h3>What you get with LANDLOGY Intelligence</h3><p>A research-first approach to every property decision.</p><ul className="intel-features">{['Location and market demand reports', 'Price trend analysis across micro-markets', 'Rental yield and capital growth estimates', 'Infrastructure and development pipeline impact', 'Verified ownership and title clarity checks', 'RERA compliance and regulatory checks', 'Comparable sales and active listing analysis', 'Direct access to investment-grade opportunities'].map(x => <li key={x}><Check size={15} />{x}</li>)}</ul><a href="#contact" className="btn btn-white" style={{ marginTop: 28, width: '100%', justifyContent: 'center' }}>Get Investment Report <ArrowUpRight size={16} /></a></div></div></div></section>
        <section id="listings" className="sec-slate"><div className="container"><Head eyebrow="Featured Properties" title="Verified opportunities across India." text="A curated selection of residential, commercial and investment properties — each reviewed and verified by our team." /><div className="listings-filter">{['all', 'sale', 'rent', 'invest'].map(x => <button key={x} onClick={() => setFilter(x)} className={`ftag ${filter === x ? 'active' : ''}`}>{x === 'all' ? 'All' : x === 'sale' ? 'For Sale' : x === 'rent' ? 'For Rent' : 'Investment'}</button>)}</div><div className="listings-grid">{visibleProps.map((p, i) => <article className="prop-card tilt" data-reveal key={p.title}><div className="prop-img">{p.icon}<span className={`prop-badge ${p.category}`}>{p.badge}</span><div className="image-shine" /></div><div className="prop-body"><h3>{p.title}</h3><p className="prop-loc">{p.location}</p><div className="prop-specs">{p.specs.map(s => <span key={s}>{s}</span>)}</div><div className="prop-price">{p.price} <small>{p.priceSuffix}</small></div></div><div className="prop-footer"><a href="#contact">Enquire <ArrowUpRight size={14} /></a>{p.verified !== false && <span className="verified-tag"><ShieldCheck size={13} /> Verified</span>}</div></article>)}</div><div className="listings-cta"><Magnetic href="#contact" className="btn-primary">View All Properties & Enquire →</Magnetic></div></div></section>
        <section id="network" className="sec-navy"><div className="container"><Head dark eyebrow="Professional Network" title="Built for the entire real estate community." text="Owners, buyers, tenants, investors, developers, brokers and service partners — all connected on one trusted platform." /><div className="network-grid">{site.networks.map((n) => <article className="ncard tilt" data-reveal key={n.title}><div className="ncard-icon">{n.icon}</div><h3>{n.title}</h3><p>{n.desc}</p><ArrowUpRight className="narrow" size={18} /></article>)}</div></div></section>
        <section id="team" className="sec-slate"><div className="container"><Head eyebrow="Our People" title="People behind every property decision." text="From property assessment and market positioning to presentation, verification and client support, LANDLOGY brings focused expertise to each stage of the selling journey." /><div className="team-grid">{site.team.map((m) => <article className="team-card tilt" data-reveal key={m.name}><div className="team-avatar">{m.initial}{m.photo && <img className="team-photo" src={m.photo} alt={m.name} loading="lazy" onError={e => e.currentTarget.remove()} />}</div><div className="team-card-body"><h3>{m.name}</h3><p className="role">{m.role}</p><p className="focus">{m.detail}</p><p className="team-bio">{m.bio}</p></div><div className="team-card-meta"><span className="team-marker">{m.marker}</span></div></article>)}</div><div className="trust-strip reveal" data-reveal><span>Seller-first advisory</span><span>Structured property review</span><span>Cross-functional support</span><span>Private client experience</span></div><div className="team-cta reveal" data-reveal><a href="#contact" className="btn btn-primary">Talk to the LANDLOGY Team <ArrowUpRight size={15} /></a></div></div></section>
        <section className="sec-slate"><div className="container"><Head eyebrow="Client Stories" title="Trusted by buyers, investors and developers." /><div className="testi-grid">{site.reviews.map((r) => <article className="testi-card" data-reveal key={r.name}><div className="stars">{'★'.repeat(r.rating || 5)}</div><blockquote>“{r.text}”</blockquote><div className="testi-author"><div className="tav">{r.initial}</div><div><strong>{r.name}</strong><span>{r.role}</span></div></div></article>)}</div></div></section>
        <section id="contact"><div className="container"><Head eyebrow="Get in Touch" title="Let's work on your real estate goal." text="Whether you want to buy, sell, rent, invest, research or develop — our team is ready to help." /><div className="contact-grid"><div data-reveal><div className="contact-info"><Info icon={<Phone />} title="Call / WhatsApp"><a href={`tel:${site.contact.phoneRaw}`}>{site.contact.phone}</a></Info><Info icon={<Mail />} title="Email Us"><a href={`mailto:${site.contact.email}`}>{site.contact.email}</a></Info><Info icon={<MapPin />} title="Head Office">{site.contact.address}</Info><Info icon={<Clock />} title="Working Hours">{site.contact.workingHours.weekday}<br />{site.contact.workingHours.sunday}</Info></div><div className="map-wrap"><iframe title="LANDLOGY location" src={site.contact.mapEmbedUrl} loading="lazy" /></div></div><div className="contact-form" data-reveal><h3>Send us a message</h3><p>Our team will get back to you within 24 hours.</p><Form /></div></div></div></section>
        <footer><div className="container"><div className="foot-grid"><div className="foot-brand"><strong>LANDLOGY</strong><small>REAL ESTATE ECOSYSTEM</small><p>India’s trusted platform for property buying, selling, renting, investment intelligence and professional broker network.</p></div><div className="foot-col"><h4>Services</h4>{['Buy Property', 'Sell Property', 'Rent & Lease', 'Investment Intelligence','Land Aggregation', 'Property Verification', '3D visualization', 'Development R&D'].map(x => <a href="#services" key={x}>{x}</a>)}</div><div className="foot-col"><h4>Platform</h4>{['Browse Properties', 'Broker Network', 'Market Research', 'Our Team', 'Contact Us'].map(x => <a href="#contact" key={x}>{x}</a>)}</div><div className="foot-col"><h4>Connect</h4><a href={`tel:${site.contact.phoneRaw}`}>📞 {site.contact.phone}</a><a href={`mailto:${site.contact.email}`}>✉ {site.contact.email}</a><a href={`https://wa.me/${site.contact.phoneRaw.replace(/\D/g, '')}`} target="_blank">💬 WhatsApp Us</a><a href="#">LinkedIn</a><a href="#">Instagram</a></div></div><div className="foot-bottom"><p>© 2026 LANDLOGY. All rights reserved.</p><p>Information provided is for general guidance and does not constitute legal, financial or investment advice.</p></div></div></footer><a className="whatsapp-btn" href={`https://wa.me/${site.contact.phoneRaw.replace(/\D/g, '')}?text=${encodeURIComponent(site.contact.whatsappMessage)}`} target="_blank" aria-label="Chat on WhatsApp"><MessageCircle /></a><div className="cursor-glow" /></>
}
function ClientLogin() {
    const [showPassword, setShowPassword] = useState(false), [notice, setNotice] = useState('');
    const submit = e => { e.preventDefault(); setNotice('Demo mode: client authentication will be connected in a later phase.'); };
    return <main className="client-login-page"><div className="client-login-art"><a href="/" className="portal-brand"><img src={logo} alt="LANDLOGY" /><span>Private client workspace</span></a><div className="client-login-message"><span className="eyebrow">LANDLOGY Client Portal</span><h1>Your property journey, with clarity.</h1><p>A private space for LANDLOGY clients and property owners to follow progress, review documents and stay connected with our team.</p><div className="login-art-rule" /></div><small>Research · Verify · Transact · Grow</small></div><section className="client-login-panel" aria-labelledby="client-login-title"><a className="login-back" href="/"><ChevronRight size={15} /> Back to LANDLOGY</a><div className="login-card"><div className="login-card-heading"><div className="portal-mark">L</div><span className="eyebrow">Secure access</span><h2 id="client-login-title">Client Login</h2><p>For LANDLOGY clients and property owners.</p></div><form onSubmit={submit} className="client-form"><label htmlFor="client-email">User ID / Email</label><input id="client-email" name="email" type="email" placeholder="Enter your registered email" autoComplete="email" /><label htmlFor="client-password">Password</label><div className="password-field"><input id="client-password" name="password" type={showPassword ? 'text' : 'password'} placeholder="Enter your password" autoComplete="current-password" /><button type="button" onClick={() => setShowPassword(v => !v)} aria-label={showPassword ? 'Hide password' : 'Show password'}>{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div><div className="login-options"><label className="remember-option"><input type="checkbox" /> <span>Remember me</span></label><a href="mailto:hello@landlogy.in?subject=Client%20portal%20support">Forgot password?</a></div>{notice && <p className="demo-notice" role="status"><CircleHelp size={16} /> {notice}</p>}<button className="btn btn-primary login-submit" type="submit">Sign in to portal <ArrowUpRight size={16} /></button></form><p className="login-support">Need help accessing your account? <a href="mailto:hello@landlogy.in?subject=Client%20portal%20support">Contact LANDLOGY support</a></p></div></section></main>
}

function ClientPortal() {
    const property = CLIENT_MOCK.property;
    const timeline = [['Enquiry Submitted', '05 Sep 2026'], ['LANDLOGY Review', 'In progress'], ['Property Verified', 'Upcoming'], ['Property Processing', 'Upcoming'], ['Property Active', 'Upcoming'], ['Sold', 'Upcoming']];
    return <main className="portal-page"><header className="portal-topbar"><a href="/" className="portal-logo"><img src={logo} alt="LANDLOGY" /><span>Client Portal</span></a><div className="portal-top-actions"><button className="icon-button" aria-label="Notifications"><Bell size={18} /><i /></button><div className="portal-account"><span className="account-avatar">AM</span><span><strong>{CLIENT_MOCK.name}</strong><small>Property owner</small></span></div><a href="/" className="portal-logout"><LogOut size={16} /> <span>Log out</span></a></div></header><div className="portal-shell"><aside className="portal-sidebar"><div className="sidebar-context"><span className="eyebrow">Private workspace</span><strong>{CLIENT_MOCK.accountId}</strong></div><nav aria-label="Client portal navigation"><a className="active" href="#dashboard"><LayoutDashboard size={17} /> Dashboard</a><a href="#property"><Home size={17} /> My Property</a><a href="#status"><TrendingUp size={17} /> Property Status</a><a href="#documents"><FileText size={17} /> Documents</a><a href="#support"><CircleHelp size={17} /> Messages / Support</a><a href="#profile"><UserRound size={17} /> Profile</a></nav><a className="sidebar-logout" href="/"><LogOut size={17} /> Log out</a></aside><div className="portal-content"><div className="portal-mobile-heading"><span className="eyebrow">Client Portal</span><button className="icon-button" aria-label="Notifications"><Bell size={18} /><i /></button></div><section id="dashboard" className="portal-welcome"><div><span className="eyebrow">Your LANDLOGY workspace</span><h1>Welcome back, {CLIENT_MOCK.name.split(' ')[0]}.</h1><p>Here is the latest view of your property journey with LANDLOGY.</p></div><span className="last-synced">Last updated {property.updated}</span></section><section className="portal-overview-grid"><article className="portal-status-card"><div className="status-card-head"><div><span className="eyebrow">Current property status</span><h2>Under review</h2></div><span className="status-dot"><Check size={16} /></span></div><p>Our team is reviewing your property details and submitted documents. We will update this space as the review progresses.</p><div className="status-progress"><span style={{ width: '27%' }} /></div><div className="status-progress-labels"><span>Submitted</span><strong>Under Review</strong><span>Active</span></div></article><article className="portal-mini-card"><span className="eyebrow">Your property</span><h3>{property.title}</h3><p><MapPin size={14} /> {property.location}</p><dl><div><dt>Reference ID</dt><dd>{property.reference}</dd></div><div><dt>Property type</dt><dd>{property.type}</dd></div></dl><a href="#property" className="text-link">View property details <ArrowUpRight size={14} /></a></article></section><section id="property" className="portal-section"><div className="section-heading"><div><span className="eyebrow">My property</span><h2>A clear view of your listing</h2></div><a href="#property" className="quiet-button">View details <ArrowUpRight size={14} /></a></div><article className="owner-property-card"><img src={property.image} alt="Property preview" /><div className="owner-property-info"><div className="property-title-row"><div><span className="property-owner-tag"><UserRound size={13} /> Your property</span><h3>{property.title}</h3><p><MapPin size={15} /> {property.location}</p></div><span className="status-pill">Under review</span></div><div className="property-facts"><span><small>Property type</small>{property.type}</span><span><small>Reference ID</small>{property.reference}</span><span><small>Price guidance</small>{property.price}</span><span><small>Last updated</small>{property.updated}</span></div></div></article></section><section id="status" className="portal-section"><div className="section-heading"><div><span className="eyebrow">Read-only progress</span><h2>Property status timeline</h2></div><span className="section-note">Current stage: Under review</span></div><div className="timeline">{timeline.map(([label, date], index) => <div className={`timeline-item ${index === 0 ? 'complete' : index === 1 ? 'current' : ''}`} key={label}><div className="timeline-marker">{index === 0 ? <Check size={14} /> : String(index + 1).padStart(2, '0')}</div><div><strong>{label}</strong><span>{date}</span>{index === 1 && <p>LANDLOGY is checking the submitted property details and documents.</p>}</div></div>)}</div></section><section id="documents" className="portal-section documents-section"><div className="section-heading"><div><span className="eyebrow">Your files</span><h2>Documents</h2></div><span className="section-note">Frontend preview</span></div><div className="document-list">{CLIENT_MOCK.documents.map(doc => <div className="document-row" key={doc.name}><span className="document-icon"><FileText size={18} /></span><div><strong>{doc.name}</strong><span>{doc.type} · Uploaded {doc.date}</span></div><span className={`document-status ${doc.status === 'In review' ? 'review' : ''}`}>{doc.status}</span><button className="icon-button document-action" type="button" aria-label={`View ${doc.name}`}><Download size={17} /></button></div>)}</div></section><div className="portal-lower-grid"><section id="support" className="portal-support"><span className="eyebrow">Need a hand?</span><h2>We are here to help.</h2><p>Ask about your property status or speak with the LANDLOGY team.</p><div className="support-actions"><a href="mailto:hello@landlogy.in?subject=Property%20status%20support"><Mail size={16} /> Email support</a><a href="tel:+910000000000"><Phone size={16} /> Call LANDLOGY</a></div></section><section id="profile" className="portal-profile"><div className="section-heading"><div><span className="eyebrow">Account</span><h2>Your profile</h2></div><UserRound size={19} /></div><dl className="profile-list"><div><dt>Client name</dt><dd>{CLIENT_MOCK.name}</dd></div><div><dt>Email</dt><dd>{CLIENT_MOCK.email}</dd></div><div><dt>Phone</dt><dd>{CLIENT_MOCK.phone}</dd></div><div><dt>City</dt><dd>{CLIENT_MOCK.city}</dd></div></dl></section></div><p className="portal-demo-note">This portal is a frontend prototype. Property status, documents and account details will connect to LANDLOGY services in a future phase.</p></div></div><nav className="portal-mobile-nav" aria-label="Mobile portal navigation"><a className="active" href="#dashboard"><LayoutDashboard size={17} /> Home</a><a href="#property"><Home size={17} /> Property</a><a href="#status"><TrendingUp size={17} /> Status</a><a href="#documents"><FileText size={17} /> Files</a><a href="#profile"><UserRound size={17} /> Profile</a></nav></main>
}

function SellerCapabilities({ site }) {
    return <><section id="land-aggregation" className="capability-section land-capability"><div className="container capability-grid"><div className="capability-copy" data-reveal><span className="eyebrow">LANDLOGY Signature Capability</span><h2>Land Aggregation</h2><p>Bringing fragmented land opportunities together through structured property intelligence, verification and coordinated development or sale strategy.</p><div className="capability-points"><span><strong>01</strong>Identify connected land opportunities</span><span><strong>02</strong>Organise property and location information</span><span><strong>03</strong>Shape a coordinated next-step strategy</span></div><a href="#contact" className="btn btn-primary">Discuss Land Strategy <ArrowUpRight size={15} /></a></div><div className="land-visual" data-reveal><div className="land-map-grid" /><div className="land-parcel parcel-a">A</div><div className="land-parcel parcel-b">B</div><div className="land-parcel parcel-c">C</div><div className="land-parcel parcel-d">D</div><div className="land-connect connect-one" /><div className="land-connect connect-two" /><span className="land-label label-location">LOCATION INTELLIGENCE</span><span className="land-label label-verified">VERIFICATION LAYER</span><span className="land-coordinates">LAND / STRATEGY / CONTEXT</span></div></div></section><section id="visualization" className="capability-section visualization-section"><div className="container visualization-grid"><div className="visualization-stage" data-reveal><div className="viz-frame"><div className="viz-building viz-building-one"><i /><i /><i /><i /></div><div className="viz-building viz-building-two"><i /><i /><i /></div><span className="viz-hotspot hotspot-one">01</span><span className="viz-hotspot hotspot-two">02</span><span className="viz-axis">SPATIAL VIEW / 03</span></div><div className="viz-controls"><span>PROPERTY PRESENTATION CONCEPT</span><span>◀ &nbsp; 01 / 03 &nbsp; ▶</span></div></div><div className="capability-copy" data-reveal><span className="eyebrow">Digital Property Presentation</span><h2>3D Property Visualization</h2><p>Where applicable, LANDLOGY can communicate a property or space through immersive visualization concepts that help people understand form, layout, multiple angles and points of interest.</p><div className="visualization-list"><span><strong>◈</strong>Multiple-angle spatial understanding</span><span><strong>◈</strong>Property presentation and walkthrough concepts</span><span><strong>◈</strong>Clearer communication of space and potential</span></div><small>Capability concept. Availability depends on the property brief and agreed scope.</small></div></div></section><section className="capability-section location-section"><div className="container location-grid"><div className="location-copy" data-reveal><span className="eyebrow">Real Contact / Presence</span><h2>Start with a real conversation.</h2><p>Share your property details through the enquiry form or use the contact information below. The location shown uses the project’s current configured map data.</p><div className="location-details"><strong>{site.contact.phone}</strong><span>{site.contact.email}</span><span>{site.contact.address}</span></div><a href="#contact" className="text-link">Start a property enquiry <ArrowUpRight size={14} /></a></div><div className="map-wrap capability-map" data-reveal><iframe title="LANDLOGY configured location" src={site.contact.mapEmbedUrl} loading="lazy" /></div></div></section></>;
}

function SellerLanding() {
    const [menu, setMenu] = useState(false);
    const { site, error, load } = useSiteData();
    useReveal(!!site);
    useEffect(() => { document.body.classList.toggle('menu-open', menu); return () => document.body.classList.remove('menu-open') }, [menu]);
    useEffect(() => { if (!site) return; const mountPoint = document.querySelector('.seller-landing #why-sell'); if (!mountPoint || document.querySelector('.seller-capabilities-root')) return; const rootElement = document.createElement('div'); rootElement.className = 'seller-capabilities-root'; mountPoint.parentNode.insertBefore(rootElement, mountPoint); const root = createRoot(rootElement); root.render(<SellerCapabilities site={site} />); return () => { root.unmount(); rootElement.remove() } }, [site]);
    useEffect(() => { const addCapabilityLinks = () => { const nav = document.querySelector('.seller-landing .navlinks'); if (nav && !nav.querySelector('[href="#land-aggregation"]')) { nav.insertAdjacentHTML('beforeend', '<a href="#land-aggregation">Land Aggregation</a><a href="#visualization">3D Visualization</a>') } const menuRoot = document.querySelector('.seller-landing .seller-mobile-menu'); if (menuRoot && !menuRoot.querySelector('[href="#land-aggregation"]')) { menuRoot.insertAdjacentHTML('afterbegin', '<a href="#land-aggregation">Land Aggregation</a><a href="#visualization">3D Visualization</a>') } }; addCapabilityLinks(); const observer = new MutationObserver(addCapabilityLinks); observer.observe(document.body, { childList: true, subtree: true }); return () => observer.disconnect() }, [menu]);
    if (!site) return <div className="seller-loading">{error ? <><strong>LANDLOGY is temporarily unavailable.</strong><button className="btn btn-primary" onClick={load}>Retry</button></> : 'Loading LANDLOGY...'}</div>;
    const services = [
        ['🔍', 'Property Assessment', 'Understand your property, its location and its position before deciding the right next step.'],
        ['📊', 'Pricing & Positioning', 'Use a considered view of the property and market context to prepare a sensible selling strategy.'],
        ['✓', 'Verification Support', 'Organise relevant property information and documentation before serious promotion begins.'],
        ['▣', 'Professional Presentation', 'Present the property clearly with a considered description and materials that communicate its value.'],
        ['◎', 'Relevant Buyer Reach', 'Connect the property with prospective buyers whose requirements are relevant to what it offers.'],
        ['↗', 'Negotiation & Closing Support', 'Receive guidance through offers, discussions and the closing stages of the sale.'],
        ['◫', 'Land Aggregation', 'Bring fragmented land opportunities together through structured property intelligence, verification and coordinated strategy.'],
        ['◇', '3D Property Visualization', 'Present property and space through immersive visualization concepts where multiple angles and spatial understanding add value.'],
        ['▦', 'Property Intelligence & Presentation', 'Shape a clearer property story using location context, structured information and high-quality presentation.']
    ];
    const steps = [
        ['01', 'Tell Us About Your Property', 'Share the essentials so LANDLOGY can understand your property and selling objective.'],
        ['02', 'LANDLOGY Reviews Your Enquiry', 'Our team reviews the connection request and determines the appropriate next conversation.'],
        ['03', 'Property Assessment & Verification', 'Relevant property information and documentation are understood and organised.'],
        ['04', 'Selling Strategy & Preparation', 'We discuss positioning, presentation and practical preparation for the market.'],
        ['05', 'Marketing, Buyer Reach & Support', 'The property can move forward with relevant outreach and support through the sale.']
    ];
    return <main className="seller-landing"><nav><div className="container nav-inner"><a className="brand" href="#hero"><img src={logo} alt="LANDLOGY" /></a><div className="navlinks"><a href="#how-it-works">How It Works</a><a href="#services">Our Services</a><a href="#why-sell">Why LANDLOGY</a><a href="#team">Our Team</a><a href="#contact">Contact</a></div><div className="nav-cta"><a href={`tel:${site.contact.phoneRaw}`} className="btn btn-outline"><Phone size={15} /> Call Us</a><a href="/client-login" className="btn btn-client-login">Client Login</a><a href="#contact" className="btn btn-primary">List Your Property <ArrowUpRight size={15} /></a></div><button className="ham" type="button" onClick={() => setMenu(v => !v)} aria-label={menu ? 'Close menu' : 'Open menu'} aria-expanded={menu}>{menu ? <X /> : <Menu />}</button></div></nav>{menu && <div className="mobile-menu seller-mobile-menu"><a href="#how-it-works" onClick={() => setMenu(false)}>How It Works</a><a href="#services" onClick={() => setMenu(false)}>Our Services</a><a href="#why-sell" onClick={() => setMenu(false)}>Why LANDLOGY</a><a href="#team" onClick={() => setMenu(false)}>Our Team</a><a href="#contact" onClick={() => setMenu(false)}>Contact</a><a className="client-login-link" href="/client-login">Client Login</a><a className="mobile-primary-link" href="#contact" onClick={() => setMenu(false)}>List Your Property <ArrowUpRight size={15} /></a></div>}<header id="hero" className="seller-hero"><div className="seller-hero-backdrop" /><div className="container seller-hero-inner"><div className="seller-hero-copy" data-reveal><span className="eyebrow">For Property Owners Across India</span><h1>Sell Your Property<br /><span>With Confidence.</span></h1><p>LANDLOGY helps property owners sell through a structured process, considered positioning, clear presentation and support at every important step.</p><div className="hero-actions"><a href="#contact" className="btn btn-primary">List Your Property <ArrowUpRight size={16} /></a><a href="#how-it-works" className="btn btn-outline">See How It Works <ChevronRight size={16} /></a></div><div className="seller-trust-strip"><span>Research-led approach</span><span>Verification support</span><span>Professional presentation</span><span>Guided selling process</span></div></div><div className="hero-card seller-enquiry-card" data-reveal><div className="hero-card-title">Tell Us About Your Property</div><p className="seller-card-sub">Start with a property-selling enquiry. LANDLOGY will review the information you share.</p><Form hero /></div></div></header><section className="seller-pain-section"><div className="container"><div className="sec-head" data-reveal><span className="eyebrow">A Better Starting Point</span><h2>Selling property should feel clearer.</h2><p>Owners often need help understanding the right price, preparing information and reaching relevant buyers. LANDLOGY brings those conversations into one structured process.</p></div><div className="seller-pain-grid">{['Uncertainty about the right price', 'Unqualified or irrelevant enquiries', 'Property information and documents', 'Presenting the property properly', 'Negotiation and closing decisions', 'Knowing the right next step'].map((item, index) => <article className="seller-pain-card" data-reveal key={item}><span>0{index + 1}</span><h3>{item}</h3><p>Start with a clearer, more informed way to approach this part of selling.</p></article>)}</div></div></section><div className="seller-trust-band"><div className="container seller-trust-grid">{[['Research', 'Decisions begin with understanding.'], ['Verification', 'Relevant information is organised early.'], ['Reach', 'Focus on relevant prospective buyers.'], ['Support', 'Guidance continues through the process.']].map(([title, text]) => <div data-reveal key={title}><strong>{title}</strong><span>{text}</span></div>)}</div></div><section id="how-it-works" className="sec-slate"><div className="container"><div className="sec-head seller-centered-head" data-reveal><span className="eyebrow">The Selling Process</span><h2>A clearer way to sell your property.</h2><p>Five high-level steps explain what happens from your first enquiry through preparation and support.</p></div><div className="seller-steps">{steps.map(([number, title, text]) => <article data-reveal key={number}><span>{number}</span><h3>{title}</h3><p>{text}</p></article>)}</div></div></section><section id="services"><div className="container"><div className="sec-head" data-reveal><span className="eyebrow">What LANDLOGY Helps With</span><h2>Seller-focused support, end to end.</h2><p>Every service is designed around helping a property owner make informed decisions and move forward with confidence.</p></div><div className="services-grid">{services.map(([icon, title, text], index) => <article className="svc-card seller-service-card" data-reveal key={title}><div className="svc-icon">{icon}</div><h3>{title}</h3><p>{text}</p><a href="#contact" className="svc-link">Discuss this with us <ArrowUpRight size={14} /></a><span className="card-index">0{index + 1}</span></article>)}</div></div></section><section id="why-sell" className="sec-slate"><div className="container seller-why-grid"><div data-reveal><span className="eyebrow">Why Property Owners Choose LANDLOGY</span><h2>Selling with better clarity and support.</h2><p className="seller-why-intro">LANDLOGY is a service for owners who want a thoughtful selling process rather than simply placing a listing and waiting.</p>{['Research before recommendation', 'Verification before promotion', 'Clearer property presentation', 'Human guidance when decisions matter'].map((title, index) => <div className="seller-why-row" key={title}><span>{index + 1}</span><div><h3>{title}</h3><p>We keep the focus on understanding your property, your objective and the next appropriate step.</p></div></div>)}</div><div className="seller-why-panel" data-reveal><span className="eyebrow">A private client journey later</span><h3>Stay informed as your property progresses.</h3><p>After LANDLOGY reviews and accepts a client enquiry, approved clients may later receive secure access to a private portal for their own property information and status.</p><a href="/client-login" className="btn btn-white">Client Login <ArrowUpRight size={15} /></a></div></div></section><section id="team"><div className="container"><div className="sec-head" data-reveal><span className="eyebrow">Our Approach</span><h2>Professional support, focused on your property.</h2><p>LANDLOGY brings the relevant conversations together so property owners can understand what is needed before moving ahead.</p></div><div className="seller-team-note" data-reveal><div className="portal-mark">L</div><div><h3>One clear conversation to begin</h3><p>Tell us about your property, location and selling objective. The LANDLOGY team will review your enquiry before any later client access or property workflow begins.</p></div></div></div></section><section id="contact"><div className="container"><div className="sec-head" data-reveal><span className="eyebrow">Start With A Conversation</span><h2>Tell us about your property.</h2><p>Whether you are ready to begin or still understanding your options, share the basics and LANDLOGY will review your enquiry.</p></div><div className="contact-grid"><div data-reveal><div className="contact-info"><Info icon={<Phone />} title="Call / WhatsApp"><a href={`tel:${site.contact.phoneRaw}`}>{site.contact.phone}</a></Info><Info icon={<Mail />} title="Email Us"><a href={`mailto:${site.contact.email}`}>{site.contact.email}</a></Info><Info icon={<MapPin />} title="Head Office">{site.contact.address}</Info><Info icon={<Clock />} title="Working Hours">{site.contact.workingHours.weekday}<br />{site.contact.workingHours.sunday}</Info></div></div><div className="contact-form" data-reveal><h3>Property-Selling Enquiry</h3><p>Share the essentials of your property and how LANDLOGY can help.</p><Form /></div></div></div></section><footer><div className="container"><div className="foot-grid"><div className="foot-brand"><strong>LANDLOGY</strong><small>PROPERTY SELLING SUPPORT</small><p>A structured, research-led service for property owners who want to sell with confidence.</p></div><div className="foot-col"><h4>For Property Owners</h4><a href="#contact">List Your Property</a><a href="#services">Our Services</a><a href="#how-it-works">How It Works</a><a href="#why-sell">Why LANDLOGY</a></div><div className="foot-col"><h4>Client Access</h4><a href="/client-login">Client Login</a><a href="#contact">Contact LANDLOGY</a></div><div className="foot-col"><h4>Connect</h4><a href={`tel:${site.contact.phoneRaw}`}>{site.contact.phone}</a><a href={`mailto:${site.contact.email}`}>{site.contact.email}</a></div></div><div className="foot-bottom"><p>© 2026 LANDLOGY. All rights reserved.</p><p>Information provided is for general guidance only.</p></div></div></footer></main>;
}

function PublicRoute() {
    return <SellerLanding />;
}
function App() { const path = window.location.pathname.replace(/\/$/, '') || '/'; if (path === '/client-login') return <ClientLogin />; if (path === '/client-portal') return <ClientPortal />; return <PublicRoute />; }
function Head({ eyebrow, title, text, dark }) { return <div className="sec-head" data-reveal><div className="eyebrow">{eyebrow}</div><h2 className={dark ? 'dark-title' : ''}>{title}</h2>{text && <p>{text}</p>}</div> }; function Info({ icon, title, children }) { return <div className="cinfo-block"><div className="cinfo-icon">{icon}</div><div><strong>{title}</strong><p>{children}</p></div></div> }
createRoot(document.getElementById('root')).render(<App />);
